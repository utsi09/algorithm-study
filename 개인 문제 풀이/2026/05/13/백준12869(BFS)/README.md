bfs가 i,j가 상태, dy dx가 각 i와 j에 취할 변화값, visited는 최단거리라면

뮤탈리스크 문제에서는 각 유닛 a,b,c가 상태, 3p3 순열에 대한 조합이 취할 변화값, visited가 몇번 공격했는지에 대한 레이어 레벨이다

출력은 0,0,0이 됐을때 레이어레벨 즉 visited값을 출력하면 된다

0이하로 내려가지않게 max(0, 처리하는것이 포인트이다
